import { Entity } from "../components/entity";
import { C_Player } from "../components/component";
import { System } from "./system";

export class S_PlayerGestion implements System {

    constructor() {};

    run() : void {
        
    }

    // var sendedEntities : Array<Entity>;

    // entities.forEach((entity) => {
    //     if (entity.components.Player == typeof Player) {
    //         sendedEntities.push(entity);
    //     }
    // });

    // return sendedEntities;
}